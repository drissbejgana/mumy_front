# Base URL of the independent backend API (see /backend).
VITE_API_BASE_URL="http://localhost:4000"

# Google OAuth Client ID — must match the backend's GOOGLE_CLIENT_ID (same value, both
# sides need it). From Google Cloud Console > APIs & Services > Credentials.
VITE_GOOGLE_CLIENT_ID=""
